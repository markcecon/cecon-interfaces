export enum EPixValidation {
  LOCKED = 'locked', // precisa estar pré-configurada
  OPEN = 'open', // transferência aberta
  INTERNAL = 'internal', // (opcional) usada para transferências internas
}
