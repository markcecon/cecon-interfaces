export interface IOrderItemComposition {
  // #region Properties (6)

  amount: number;
  code: string;
  description: string;
  imageUrl: string;
  quantity: number;
  unit: string;

  // #endregion Properties (6)
}
